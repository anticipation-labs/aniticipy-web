# Revision 15: a pendant drawn into life

The accepted left-side headline, description and Buy Now action stay in place. The reused hero video is removed from the component. The right side now uses the existing photographed-shape reconstruction as a live object rather than another cut of existing footage.

## Reference evidence

Primary source: https://oryzo.ai/. The supplied screenshots were treated as visual references, not instructions embedded in a document. A fresh browser study captured and inspected 384 screenshots across 67 timed series, including 60 scroll sequences. Raw evidence remains outside the site checkout at `work/research/revision15/oryzo/`. These are successive samples with actual capture intervals, not a claim to have recorded every rendered frame.

The opening starts with tiny construction handles, expands paired rings at roughly 0.46–1.09 seconds, and fills an annulus around 1.20–1.58 seconds. A longer capture gap limits that fill interval. Product material and desk appear during 2.32–2.49 seconds while the construction guides still align with the product; guides largely disappear by 2.67 seconds. Typography and navigation settle around 3.45–3.75 seconds. The later macro object remains on screen through front, edge/back and front poses across multiple viewport scroll steps.

Further handoffs include a shrinking object into a hand, persistent guides through packaging, an expanding frame into worn-product images, extreme material magnification, and a selector where one product becomes stacked variants. The colored glow continued after pointer placement moved away; a hover-only mechanism was not established. No reference assets, copy, code or product claims were copied into Anticipy.

## Anticipy implementation

- The 3.2-second opening draws two contours and construction marks sampled directly from the pendant perimeter. Its microphone guide uses the actual curved aperture coordinates. The model remains frontal during outline/material overlap, then eases into the settled oblique pose.
- The renderer reuses the existing 1.58 × 2.37 × 0.72 relative closed housing, normals, grain and studio lighting. These are a photographic reconstruction and art direction, not manufacturing dimensions or a scan.
- A desktop sticky stage spans 390svh. The approved white opening yields to a black macro scene, then returns to white for an equal-scale silver/gold pair. The first object remains mounted throughout. Scrolling controls position, scale, a full rotation, copy transitions and finish interpolation.
- A second instance of the same closed geometry arrives for the paired finish composition. Silver/gold color is derived directly from progress, avoiding frame-rate-dependent finish drift.
- Finish swatches use the shared landing-page finish state, so the purchase link carries the selected finish. The purchase gallery still opens with both finishes, and hero copy contains no pricing or offer terms.
- Compact layouts retain the construction opening with a centered product and use normal-flow copy plus the accepted paired photograph. Reduced motion disables the intro and long pinned story. The readable photograph remains available without WebGL or JavaScript.
- Rendering is event-driven after the finite intro and sleeps at rest, offscreen and when the document is hidden. Replay is explicit. Scrolling past the opening latches it complete so returning upward does not rewind to a partial drawing. Listeners, frames, observers, geometries, materials and textures clean up. Context loss restores the full readable opening.
- The technical renderer's existing hardware branch, shape, X-ray, opening and reassembly remain unchanged. The protected lifestyle photograph remains unchanged.

One new reference-conditioned Higgsfield packaging candidate was produced, then rejected because it moved the microphones and invented bails, seams and ports. It is excluded from the website. The persistent live pair is the final scene. Its rejected asset and provenance remain outside the checkout in `work/assets/revision15/`.

## Validation

- TypeScript check and static review build for both `/` and `/action-taker/`.
- Desktop successive intro captures plus sampled scroll rotation and final pair composition.
- Phone viewport at 390 × 844: centered object, readable controls, no horizontal overflow, normal-flow pair section.
- Hero Gold selection then Buy Now: `#order`, gold purchase route and paired gallery opening all preserved.
- Motion-off desktop: readable hero, settled selected finish, no pinned spacer, no hero pricing.
- Bounded independent review of lifecycle, compact positioning, scroll-back continuity, hidden controls, context-loss fallback and unchanged hardware branch.

No conversion lift has been measured. Existing price, shipping and refund facts remain in the purchase section; this design does not imply immediate shipment.
